name: PR Security (SBOM + Report)

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches-ignore:
      - main
      - master

permissions:
  contents: read
  pull-requests: write

jobs:
  pr-security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # （任意）モノレポや巨大リポジトリで Trivy のキャッシュを効かせたいとき
      - name: Cache Trivy data
        uses: actions/cache@v4
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ runner.os }}

      - name: Generate SBOM (CycloneDX JSON) with Trivy (lightweight)
        run: |
          mkdir -p artifacts
          docker run --rm \
            -v "$PWD":/work \
            aquasec/trivy:latest fs \
              --format cyclonedx \
              --output /work/artifacts/sbom.json \
              /work

      # High / Critical のみの HTML レポート（人間向け）
      - name: Render HTML report (High/Critical)
        run: |
          docker run --rm \
            -v "$PWD":/work \
            aquasec/trivy:latest sbom \
              --severity HIGH,CRITICAL \
              --format template \
              --template "@contrib/html.tpl" \
              --output /work/artifacts/trivy-report-high-critical.html \
              /work/artifacts/sbom.json

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Compute summary (all severities)
        id: summary
        run: |
          docker run --rm \
            -v "$PWD":/work \
            aquasec/trivy:latest sbom \
              --format json \
              /work/artifacts/sbom.json > artifacts/trivy-result.json

          CRITICAL=$(jq '[.. | objects | .Vulnerabilities? // empty | .[]? | select(.Severity=="CRITICAL")] | length' artifacts/trivy-result.json)
          HIGH=$(jq     '[.. | objects | .Vulnerabilities? // empty | .[]? | select(.Severity=="HIGH")]     | length' artifacts/trivy-result.json)
          MEDIUM=$(jq   '[.. | objects | .Vulnerabilities? // empty | .[]? | select(.Severity=="MEDIUM")]   | length' artifacts/trivy-result.json)
          LOW=$(jq      '[.. | objects | .Vulnerabilities? // empty | .[]? | select(.Severity=="LOW")]      | length' artifacts/trivy-result.json)
          UNKNOWN=$(jq  '[.. | objects | .Vulnerabilities? // empty | .[]? | select(.Severity=="UNKNOWN")]  | length' artifacts/trivy-result.json)
          TOTAL=$((CRITICAL + HIGH + MEDIUM + LOW + UNKNOWN))

          echo "critical=$CRITICAL" >> $GITHUB_OUTPUT
          echo "high=$HIGH" >> $GITHUB_OUTPUT
          echo "medium=$MEDIUM" >> $GITHUB_OUTPUT
          echo "low=$LOW" >> $GITHUB_OUTPUT
          echo "unknown=$UNKNOWN" >> $GITHUB_OUTPUT
          echo "total=$TOTAL" >> $GITHUB_OUTPUT

      - name: Upload artifacts (HTML + SBOM + JSON)
        uses: actions/upload-artifact@v4
        with:
          name: pr-security-report
          path: |
            artifacts/trivy-report-high-critical.html
            artifacts/sbom.json
            artifacts/trivy-result.json
          if-no-files-found: error
          retention-days: 14

      # 同じタグのコメントを更新（重複投稿しない工夫）
      - name: Create/Update PR comment (summary + link to artifacts)
        uses: actions/github-script@v7
        with:
          # 文字化け対策したいときは下のコメント解除（出力を文字列に固定）
          result-encoding: string
          script: |
            const body = `
            <!-- sbom-trivy-summary -->
            🔍 **Dependency Security (SBOM-based) / PR Summary**

            - **Critical**: ${process.env.CRITICAL}
            - **High**: ${process.env.HIGH}
            - Medium: ${process.env.MEDIUM}
            - Low: ${process.env.LOW}
            - Unknown: ${process.env.UNKNOWN}
            - **Total**: ${process.env.TOTAL}

            📄 **HTML レポート (High/Critical)** は **Artifacts** からダウンロードしてください：  
            **Actions → このジョブ → Artifacts → \`pr-security-report\` → \`trivy-report-high-critical.html\`**

            🧾 SBOM (CycloneDX JSON): \`sbom.json\`  
            🧪 生データ (JSON): \`trivy-result.json\`

            > メモ: これは「PR 向けの即時チェック」です。正式な DT 更新は \`main\` マージ時のパイプラインで行われます。
            `;

            // context / github / core は github-script で自動的に利用可能
            const issue_number = context.payload.pull_request
              ? context.payload.pull_request.number
              : null;
            if (!issue_number) {
              core.info('No PR context; skipping comment.');
              return;
            }

            // 既存コメントをタグで探して更新（重複防止）
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number
            });

            const tag = '<!-- sbom-trivy-summary -->';
            const existing = comments.find(c => c.body && c.body.includes(tag));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number,
                body
              });
            }
        env:
          CRITICAL: ${{ steps.summary.outputs.critical }}
          HIGH:     ${{ steps.summary.outputs.high }}
          MEDIUM:   ${{ steps.summary.outputs.medium }}
          LOW:      ${{ steps.summary.outputs.low }}
          UNKNOWN:  ${{ steps.summary.outputs.unknown }}
          TOTAL:    ${{ steps.summary.outputs.total }}            }
